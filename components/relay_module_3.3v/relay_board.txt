# relay_board.txt

See also:
 ~/git/microcontrollers/pi_pico/projects/maranr_watering_system/watering_controller_pico_project_notes.txt

Teyleten Robot DC 1 Channel Optocoupler 3V/3.3V Relay High Level Driver Module Isolated Drive Control Board 3V/3.3V Relay Module for Arduino (Pack of 5) 
$9.99 for 5
Amazon  Order placed May 11, 2025  Order # 114-0753368-3254647 
  https://www.amazon.com/Channel-Optocoupler-Isolated-Control-Arduino/dp/B07XGZSYJV

See my diagram relay_module_3.3-5v.pdf 


=== DESCRIPTION from Amazon listing  ==========================

We are Songle original relay Size: 70mm X17mm X20mm (LxWxH) - 
Main chip: EL817, SRD-DC03V-SL-C - 
Work voltage: DC 3V/3.3V - 
Control 10A 250VAC, 10A 30VDC load 

Features - 
Instructions with the signal output;
 - Signal input port can connect microcontroller IO port, high level signal
 - Double power selection design anti-interference ability is strong, 
   photoelectric isolation protection of high reliability
 - Current wide detection range:DC0-25V input
 - A diode current protection
 - Response time is less than 20 milliseconds
 - Relay long life can absorb 100000 times continuously
 - External connection use spinning crimping terminal, make connection firmer
 - With fixed installation hole, convenient installation,

High Level Trigger:
The relay module is triggered by high level signal, 
which can be input from microcontroller IO,

Jumper Caps:You are free to select whether the relay and the signal share 
the same power supply or not by removing the jumper caps on the pins or not. 
It is recommended to share the same power supply, because that is more convenient for using.


=== MY NOTES   ============================================

Uses PC817C optoisolator chip to isolate input from relay control circuit.
Board appears to have required resistor on input side to limit current.
(220-1k, best is 330 or 470; recommended by data sheet for PC817)
Output is limited to 50mA - so board appears to have a small transistor
to raise the current to the needed 110 mA. (?)
Datasheet: PC817 optoisolator IC is designed specifically to work with different voltages on the input and output sides. It provides galvanic isolation up to 5kV, meaning there is no direct electrical connection between the input (pins 1-2) and output (pins 3-4), allowing you to safely interface circuits operating at different voltage potentials and different grounds.
Board has jumpers to allow completely separating input and relay coil sides 
of the circuits.
Optoisolator: max output side is 80V. Max power consumption 200mW.
Alternative ICs: PC816, TLP421, SFH615A

              |        |           
 Vcc  *       |        |            *  NC
               A  J1  B
 IN   *                             *  COM
              |        | 
 GND  *       |        |            *  NO
               A  J3  B

There are 2 jumpers. I labeled them J1,J2 (top,bottom of board).

They are used if you want the VCC to supply power to the relay coil
from the same power supply as used to provide IN signal.
(Probably don't need to do this - but it works).

=== JUMPERS REMOVED config  ==========================

Removed J1A,J2A.
Then used J1B to provide +3.3 v input to the coil, J2B as GND.
(IE Connected them to the right-most pins of the jumper pads.)

Input is to IN and GND screw terminals.
Provide +3.3 using a GPIO. Pico GND to GND terminal on relay.

Used the PICO 3v3 and GND (HW pins 36,38) to power the coil.
Used GPIO16 (HW pin 21) and GND on HW pin 23 to provide input
to IN,GND.

Input for IN terminal (ie input to optoisolator): at 3.3v uses 2.4 mA.
Coil at 3.3v uses 110 mA.

=== JUMPERS REMOVED config  ==========================

Putting J1, J2 back on - then provide 3.3v from Pico 3V3.
Use any Pico GND connected to the board's GND.
Connect a Pico GPIO pin (configure as output, of course) to 
the IN terminal. 
Removing J2 isolates the input side of the optoisolator.
Removing J1 just disconnects the VCC terminal- does not seem like
it is very useful(!?)

=== Details  ===========================

The J1 jumper just provides the VCC needed to run the board and
the relay - it just connects the VCC terminal to J1B. J1B provides
power to everything on the board.

The J2 jumper connects the GND teriminal to the ground side of 
all the components of the board that are 'past' the optoisolator.
Removing J2 means you supply the control signal to terminals
IN and GND. Then you must supply the 'operating power' voltage
to J1B (or to VCC if you leave J1 jumpered).

See the diagram relay_module_3.3-5v.pdf.

The LED on the module board turns on when the IN signal is provided.

The input is connected to a PC817 optoisolator pins 1,2 (left side);
output is on right side (pins 3,4). These likely go to small transistor
SMD just to right of J1B (?). The PC817 is 50mA max output, and the
coil draws 110 mA, so guessing the transistor is used as current switch
to run the coil.

Relay can handle 10A at either 250VAC or 30VDC.


Uses: Maranr Watering System for Pi Pico2


=== From Maranr Watering Controller project notes  =========================
 ~/git/microcontrollers/pi_pico/projects/maranr_watering_system/watering_controller_pico_project_notes.txt


Use Pico GPIO pin (in output mode) to provide power to IN terminal.
Connect Pico GND to the GND on the relay board.
To power the board components (transistor, relay), use VCC.

Power: 
  Input for IN terminal (ie input to optoisolator): at 3.3v uses 2.4 mA.
  Transistor+Coil at 3.3v uses 110 mA.
  Relay points ('output') can handle 10A at either 250VAC or 30VDC.

Jumpers:
Connect J1, J2 to power the board from Pico. Connect 3V3 to board's
VCC terminal. Connect a GPIO pin to IN terminal.
Connect a Pico GND to GND.

If you remove J2, it isolates the optoisolator input from the rest of
the board. Then provide signal from GPIO pin to IN terminal and Pico
GND to the GND terminal. To power the board (ie the components incl.
coil, transistor, output side of optoisolator), use VCC (or J1B if you
remove J1) and use J2B to supply GND. This allows complete separation
of the input and relay circuits.


:mode=txt:wrap=soft:
###
